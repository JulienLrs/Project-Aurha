<form method="post" action="{{ path('app_admin_home_delete', {'id': home.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home.id) }}">
    <button class="btn">Delete</button>
</form>
